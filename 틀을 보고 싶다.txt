npm install express-generator -g		//Express Framework 틀
express Hello --view==ejs

실제로 집에서 연습하고 싶으시면
mysql 설치후 
create database DImoD;
use DImoD;

CREATE TABLE user (
	id VARCHAR(15) PRIMARY KEY,
	password INT NOT NULL,
	meta VARCHAT(50) NOT NULL UNIQUE
);
으로 생성후 동작 안되시면 밑에 있는거로 동작 

ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '1234';		// my-sql client 권한 승인
FLUSH PRIVILEGES;		//권한 업로드

